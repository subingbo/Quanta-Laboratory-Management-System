<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.qt.mapper.QtDashboardMapper">

    <select id="countActiveMembers" resultType="int">
        select count(*) from sys_user
        where del_flag = '0' and is_quanta_member = '1'
    </select>

    <select id="countTodayResumes" resultType="int">
        select count(*) from qt_interview_application
        where date(create_time) = curdate()
    </select>

    <select id="countPendingReservations" resultType="int">
        select count(*) from qt_workstation_reservation
        where status = 'PENDING'
    </select>

    <select id="countPendingPayments" resultType="int">
        select count(*) from qt_clothing_order
        where status = 'SUBMITTED'
    </select>
</mapper>

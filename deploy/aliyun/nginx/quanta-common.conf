# Quanta —— Nginx 公共资源（upstream / WebSocket map）
# 该文件常驻启用：/etc/nginx/sites-enabled/quanta-common.conf

upstream quanta_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Quanta 表格与留任弹窗微调实施计划

1. 为留任确认弹窗和招新最终状态样式补充组件测试。
2. 新增 `RetainConfirmDialog.vue`，由成员页面管理选中成员、提交状态和 API 调用。
3. 在成员表格、招新表格的首列表头和单元格增加局部左侧内边距。
4. 为招新 PASS/FAIL 状态增加纯文字修饰类，并移除其背景、圆角和额外内边距。
5. 运行定向测试、完整测试、ESLint 和 Vite 生产构建。
6. 使用浏览器登录 Mock 环境，检查成员留任弹窗、两张表间距及 Pass/Out 展示并截图。

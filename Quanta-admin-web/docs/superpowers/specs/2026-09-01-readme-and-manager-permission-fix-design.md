# README 与经理层面评权限修复设计

## 目标

修复经理层在招新一面页面仍能看到“编辑面评”和“已评”的前端显示问题，并更新 README，使其准确描述当前页面、角色权限、接口完成度、Mock 使用方式和后端联调状态。

## 修改范围

### 招新权限显示

- 经理层仅在二面名单中显示“编辑面评”。
- “已评”标识与编辑入口保持一致，仅在二面显示。
- 经理层在一面仍可阅览本部门候选人简历，但不能查看或编辑面评，也不能执行 Pass、Out 或录用操作。
- 管理层和 CEO 的现有招新操作保持不变。
- 后端及 Mock 已有的二面写入校验保持不变，前端显示条件与其对齐。

### README

- 列出当前已经实现的全部主要页面。
- 修正 Mock 账号及角色能力说明。
- 使用当前后端文档中的真实接口和权限标识。
- 明确哪些功能已经实现但尚未经过真实后端联调。
- 将仍需后端确认的细节链接到 `docs/backend-integration-open-items.md`，避免在 README 中保留过时的“接口缺失”描述。
- 保留 Mock 开关和本地启动说明。

## 不在本次范围内

- 不关闭 Mock。
- 不猜测或修改尚未确认的一面评定、重置密码、分轮志愿状态和统计口径契约。
- 不重构动态路由或通用权限架构。
- 不修改用户提供的未跟踪接口文档和 Word 文件。

## 验证

- 更新组件测试，验证经理层一面不显示“编辑面评”和“已评”，二面仍显示。
- 运行相关 Vitest 测试。
- 运行完整测试、ESLint 和生产构建。


# Web 塔服收款与账号安全修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复塔服订购确认收款弹窗样式，并优化塔员端账号安全密码卡片布局与密码可见性按钮。

**Architecture:** 保持业务请求与页面结构不变，在应用入口补齐程序化组件样式；在账号安全局部样式中用现有状态类区分单卡片与双卡片布局。源码契约测试防止关键样式依赖和兼容规则被误删。

**Tech Stack:** Vue 3、Element Plus、CSS、Vitest

**Spec:** `docs/superpowers/specs/2026-09-19-web塔服收款与账号安全修复设计.md`

## Global Constraints

- 仅修改 `Quanta-admin-web` 功能代码与对应测试。
- 不修改接口、请求参数、权限规则或后端代码。
- 新生端密码与邮箱双卡片布局保持不变。

---

### Task 1: 修复确认收款弹窗样式

**Files:**
- Modify: `Quanta-admin-web/src/main.js`
- Modify: `Quanta-admin-web/src/views/clothing-orders/__tests__/clothing-orders.test.js`

**Interfaces:**
- Consumes: `ElMessageBox.confirm()` 现有调用。
- Produces: 全局加载 `element-plus/es/components/message-box/style/css`。

- [ ] **Step 1: 写入失败测试**

在塔服订单测试中读取 `src/main.js` 并断言：

```js
expect(mainSource).toContain("element-plus/es/components/message-box/style/css")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- src/views/clothing-orders/__tests__/clothing-orders.test.js`
Expected: FAIL，入口尚未包含 message-box 样式。

- [ ] **Step 3: 最小实现**

在 `src/main.js` 增加：

```js
import 'element-plus/es/components/message-box/style/css'
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- src/views/clothing-orders/__tests__/clothing-orders.test.js`
Expected: PASS。

### Task 2: 居中塔员密码卡片并移除原生重复眼睛

**Files:**
- Modify: `Quanta-admin-web/src/views/portal-security/security.css`
- Modify: `Quanta-admin-web/src/views/portal-security/__tests__/security.test.js`

**Interfaces:**
- Consumes: `.portal-security__grid.has-email-card` 区分新生双卡片布局。
- Produces: 单卡片居中规则和密码输入框原生 reveal 控件隐藏规则。

- [ ] **Step 1: 写入失败测试**

读取 `security.css` 并断言：

```js
expect(css).toContain('.portal-security__grid:not(.has-email-card)')
expect(css).toContain('justify-content: center')
expect(css).toContain('::-ms-reveal')
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- src/views/portal-security/__tests__/security.test.js`
Expected: FAIL，缺少居中和原生 reveal 隐藏规则。

- [ ] **Step 3: 最小实现**

在 `security.css` 增加：

```css
.portal-security__grid:not(.has-email-card) {
  justify-content: center;
}

.portal-security__password-field input::-ms-reveal,
.portal-security__password-field input::-ms-clear {
  display: none;
}
```

- [ ] **Step 4: 完整验证**

Run: `npm test -- src/views/portal-security/__tests__/security.test.js src/views/clothing-orders/__tests__/clothing-orders.test.js`
Expected: PASS。

Run: `npm run lint`
Expected: 退出码 0。

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 5: 提交**

```bash
git add Quanta-admin-web/src/main.js Quanta-admin-web/src/views/clothing-orders/__tests__/clothing-orders.test.js Quanta-admin-web/src/views/portal-security/security.css Quanta-admin-web/src/views/portal-security/__tests__/security.test.js
git commit -m "fix: repair order confirmation and security layout"
```
